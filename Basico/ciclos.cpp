#include <iostream>

int main() {
//FOR LOOP
//            for (initialization; condition; updation) { 
//                body of for loop
//             }


/*

* Inicialización (Initialization):** Inicializa la variable con algún valor inicial.
* Condición de prueba (Test Condition):** Esto especifica la condición de prueba. Si la condición se evalúa como verdadera (`true`), 
  entonces se ejecuta el cuerpo del ciclo. Si se evalúa como falsa (`false`), el ciclo termina.
* Expresión de actualización (Update Expression):** Después de ejecutar el cuerpo del ciclo, esta expresión incrementa o decrementa 
  la variable del ciclo por algún valor.
  
*/


}